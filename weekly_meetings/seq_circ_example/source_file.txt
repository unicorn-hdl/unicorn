// Random sequential circuit

module seqCirc() {


    //Variable A of type register with two inputs (input, state)
    wire a = val X;
    wire b = val Y;
    wire c = XOR(a,b);
    wire d = NOT(b);

    register init0 X := c;
    register init1 Y := d;

    out:c;
    out:d;

}


Main() {

    seqCirc();
    wire z = seqCirc()[c];

}